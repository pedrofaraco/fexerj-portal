/** Git short SHA and ISO build time — set at compile time (see `vite.config.js`). */
export const BUILD_COMMIT = __BUILD_COMMIT__
/** Short hash of `frontend/` tree at HEAD — parity across UAT/prod when this matches. */
export const FRONTEND_SNAPSHOT = __FRONTEND_SNAPSHOT__
export const BUILD_TIME = __BUILD_TIME__
