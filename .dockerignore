# VCS / local
.git
.gitignore
.DS_Store
.cursor
.claude

# Python
.venv
**/__pycache__
*.py[cod]
.mypy_cache
.pytest_cache
.ruff_cache
.coverage
htmlcov

# Tests and docs (not used by backend image)
tests/

# Frontend — backend image does not COPY these; nginx image builds from frontend/ via docker/Dockerfile.nginx
frontend/node_modules
frontend/dist

# Root markdown (Dockerfile does not COPY these)
*.md
